{
  "name": "nodebootstraper",
  "version": "5.0.0",
  "dependencies": {
    "body-parser": "*",
    "express": "*",
    "method-override": "*",
    "morgan": "*",
    "pug": "*",
    "serve-favicon": "*",
    "stylus": "*"
  },
  "engines": {
    "node": "*",
    "npm": "*"
  },
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
